#include "Car.hpp"

int main(int argc, char **argv)
{
    zun::Car car = zun::Car();
    car.say();
}

/*  GL      - libGL.so, Core OpenGL API
    GLU     - libGLU.so, Utility library
    GLUT    - libglut.so, Windowing + input helper */